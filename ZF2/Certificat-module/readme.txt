This module ZF2. In this case, it is a subdomain. Ie on the project, there is one basic module and several different sub-domains (modules) that use the code base.
On the project has been used assets manager for compilation less and minimization.

Functional module:
- Attachment of trained users
- Ability to create and download PDF that users were trained